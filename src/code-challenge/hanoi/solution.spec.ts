import { expect, test, describe } from "bun:test";
import { solution } from "./solution";

describe("", () => {
  test("", () => {
    expect(solution(2)).toBe([
      [1, 2],
      [1, 3],
      [2, 3],
    ]);
  });
});
